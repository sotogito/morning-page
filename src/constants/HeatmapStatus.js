export const HEATMAP_STATUS = Object.freeze({
  GRAY: 'gray',
  GREEN: 'green',
  ORANGE: 'orange',
  RED: 'red',
});

export const HOUR_THRESHOLD = Object.freeze({
  MORNING: 10,
  AFTERNOON: 14,
});
